# mnist_digit_webapp
End-to-end machine learning project that trains a CNN on the MNIST handwritten digits dataset and deploys it as a real-time web application. Users can draw digits in the browser and receive live predictions via a FastAPI backend serving a PyTorch model. Tech stack: PyTorch · FastAPI · HTML/CSS/JS · Render · GitHub Pages
